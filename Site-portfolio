<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Портфолио разработчика</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Навигация */
        nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 1rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
        }

        nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 2rem;
        }

        nav a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            transition: all 0.3s ease;
        }

        nav a:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        /* Основной контент */
        main {
            margin-top: 80px;
        }

        section {
            padding: 4rem 0;
            margin-bottom: 2rem;
        }

        .section-content {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin: 2rem 0;
        }

        /* Главная секция */
        .hero {
            text-align: center;
            color: white;
            padding: 6rem 0;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .profile-photo {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            border: 5px solid rgba(255, 255, 255, 0.3);
            margin: 0 auto 2rem;
            display: block;
            object-fit: cover;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        /* Заголовки секций */
        h2 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            text-align: center;
            color: #333;
            position: relative;
        }

        h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            margin: 1rem auto;
            border-radius: 2px;
        }

        /* Обо мне */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 3rem;
            align-items: center;
        }

        .about-text {
            font-size: 1.1rem;
            line-height: 1.8;
        }

        /* Проекты */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .project-card {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-5px);
        }

        .project-card h3 {
            color: #667eea;
            margin-bottom: 1rem;
        }

        .code-snippet {
            background: #2d3748;
            color: #e2e8f0;
            padding: 1.5rem;
            border-radius: 10px;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            overflow-x: auto;
            margin: 1rem 0;
            border-left: 4px solid #667eea;
        }

        /* Цитата */
        .quote-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-align: center;
        }

        .quote-section .section-content {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        blockquote {
            font-size: 1.5rem;
            font-style: italic;
            margin: 2rem 0;
            line-height: 1.6;
        }

        cite {
            display: block;
            margin-top: 1rem;
            font-size: 1.1rem;
            opacity: 0.8;
        }

        cite::before {
            content: "— ";
        }

        /* Контакты */
        .contacts-list {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 2rem;
            flex-wrap: wrap;
        }

        .contacts-list li {
            background: #667eea;
            padding: 1rem 2rem;
            border-radius: 25px;
            transition: all 0.3s ease;
        }

        .contacts-list li:hover {
            background: #764ba2;
            transform: translateY(-3px);
        }

        .contacts-list a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        /* Адаптивность */
        @media (max-width: 768px) {
            nav ul {
                flex-direction: column;
                gap: 1rem;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .about-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .contacts-list {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>
<body>
    <!-- Навигация -->
    <nav>
        <div class="container">
            <ul>
                <li><a href="#home">Главная</a></li>
                <li><a href="#about">Обо мне</a></li>
                <li><a href="#projects">Проекты</a></li>
                <li><a href="#quote">Цитата</a></li>
                <li><a href="#contacts">Контакты</a></li>
            </ul>
        </div>
    </nav>

    <main>
        <!-- Главная секция -->
        <section id="home" class="hero">
            <div class="container">
                <img src="https://via.placeholder.com/200x200/667eea/ffffff?text=Фото" alt="Мое фото" class="profile-photo">
                <h1>Привет! Я начинающий разработчик</h1>
                <p>Создаю веб-приложения с любовью к деталям и страстью к инновациям</p>
            </div>
        </section>

        <!-- Обо мне -->
        <section id="about">
            <div class="container">
                <div class="section-content">
                    <h2>Обо мне</h2>
                    <div class="about-content">
                        <div>
                            <img src="https://via.placeholder.com/300x200/764ba2/ffffff?text=О+разработке" alt="О разработке" style="width: 100%; border-radius: 15px;">
                        </div>
                        <div class="about-text">
                            <p>Меня зовут Алексей, и я начинающий веб-разработчик с большими амбициями. Изучаю современные технологии и создаю проекты, которые помогают людям решать реальные задачи.</p>
                            <p>Специализируюсь на фронтенд-разработке, но также изучаю бэкенд технологии. Владею HTML, CSS, JavaScript, React и другими современными инструментами разработки.</p>
                            <p>Постоянно совершенствую свои навыки, изучаю новые технологии и стремлюсь создавать качественные, удобные и красивые веб-приложения.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Проекты -->
        <section id="projects">
            <div class="container">
                <div class="section-content">
                    <h2>Мои проекты</h2>
                    <div class="projects-grid">
                        <div class="project-card">
                            <h3>Интернет-магазин</h3>
                            <p>Современный интернет-магазин с корзиной покупок, фильтрацией товаров и адаптивным дизайном.</p>
                            <div class="code-snippet">
<code>function addToCart(productId) {
    const cart = JSON.parse(localStorage.getItem('cart')) || [];
    const existingItem = cart.find(item => item.id === productId);
    
    if (existingItem) {
        existingItem.quantity += 1;
    } else {
        cart.push({ id: productId, quantity: 1 });
    }
    
    localStorage.setItem('cart', JSON.stringify(cart));
    updateCartUI();
}</code>
                            </div>
                        </div>
                        
                        <div class="project-card">
                            <h3>Планировщик задач</h3>
                            <p>Веб-приложение для управления задачами с возможностью создания, редактирования и удаления.</p>
                            <div class="code-snippet">
<code>const TaskManager = {
    tasks: [],
    
    addTask(title, description) {
        const task = {
            id: Date.now(),
            title,
            description,
            completed: false,
            createdAt: new Date()
        };
        this.tasks.push(task);
        this.renderTasks();
    }
};</code>
                            </div>
                        </div>
                        
                        <div class="project-card">
                            <h3>Погодное приложение</h3>
                            <p>Приложение для отображения текущей погоды с использованием API и красивым интерфейсом.</p>
                            <div class="code-snippet">
<code>async function getWeather(city) {
    try {
        const response = await fetch(
            `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${API_KEY}`
        );
        const data = await response.json();
        return data;
    } catch (error) {
        console.error('Ошибка получения данных о погоде:', error);
    }
}</code>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Цитата -->
        <section id="quote" class="quote-section">
            <div class="container">
                <div class="section-content">
                    <h2 style="color: white;">Вдохновляющая цитата</h2>
                    <blockquote>
                        "Программирование — это не только написание кода, это искусство решения проблем и создания решений, которые делают мир лучше."
                    </blockquote>
                    <cite>Стив Джобс</cite>
                </div>
            </div>
        </section>

        <!-- Контакты -->
        <section id="contacts">
            <div class="container">
                <div class="section-content">
                    <h2>Контакты</h2>
                    <p style="text-align: center; margin-bottom: 2rem; font-size: 1.1rem;">
                        Свяжитесь со мной для обсуждения проектов и сотрудничества
                    </p>
                    <ul class="contacts-list">
                        <li>
                            <a href="mailto:developer@example.com">
                                📧 Email
                            </a>
                        </li>
                        <li>
                            <a href="https://github.com/username" target="_blank">
                                🐙 GitHub
                            </a>
                        </li>
                        <li>
                            <a href="https://linkedin.com/in/username" target="_blank">
                                💼 LinkedIn
                            </a>
                        </li>
                        <li>
                            <a href="https://t.me/username" target="_blank">
                                📱 Telegram
                            </a>
                        </li>
                        <li>
                            <a href="https://vk.com/username" target="_blank">
                                🔵 VKontakte
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </section>
    </main>

    <script>
        // Плавная прокрутка к секциям
        document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Подсветка активной ссылки в навигации
        window.addEventListener('scroll', () => {
            const sections = document.querySelectorAll('section[id]');
            const navLinks = document.querySelectorAll('nav a[href^="#"]');
            
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (scrollY >= (sectionTop - 200)) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${current}`) {
                    link.classList.add('active');
                }
            });
        });
    </script>
</body>
</html>
